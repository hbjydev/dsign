import styled from 'styled-components';

export { Button, Input, InputGroup, Label } from './form';

export const Wrapper = styled.div`
  * {
    box-sizing: border-box;
    font-family: 'Roboto', sans-serif;
  }
`;
